// Generated by CoffeeScript 1.8.0
var expect, inNode, ragents, shelljs;

inNode = typeof Window === "undefined";

if (inNode) {
  expect = require("expect.js");
  shelljs = require("shelljs");
  ragents = require("ragents");
}

describe("package", function() {
  it("version should be a semver", function() {
    return expect(ragents.version).to.match(/^\d+\.\d+\.\d+(.*)$/);
  });
  return it("should export function `createSession()`", function() {
    return expect(ragents.createSession).to.be.a("function");
  });
});
